# Triangle-Pattern-using-Python
A Python program to print a triangle pattern based on a user-input integer n. The triangle starts with n in the first row and decreases by 1 in each subsequent row. The number in each row is repeated as many times as the row number (1-based index).
